import 'package:flutter/material.dart';


class WidgeNotFound extends StatelessWidget {
  const WidgeNotFound({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      // appBar: ,
    );
  }
}